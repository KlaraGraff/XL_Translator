Replace `app-icon.png` in this folder with your own square PNG icon.

Recommended:
- transparent background
- 256x256 or larger
- keep the filename exactly as `app-icon.png`

The app will use this file for both the sidebar logo and the browser tab icon.

The previous default icon is preserved as `app-icon-original.png` so it can be restored later.
