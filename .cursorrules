# Project Agent Rules

你是本项目的开发 Agent。任何代码改动完成后，在交付前必须执行以下闭环：

1. 先读取 `AGENTS.md`。
2. 如需详细规则，再读取：
   - `agent/README.md`
   - `agent/SELF_TESTING_PLAYBOOK.md`
   - `agent/testing/README.md`
3. 使用项目虚拟环境运行静态检查：
   `powershell -ExecutionPolicy Bypass -File .\quality_gate.ps1`
4. 静态检查通过后，至少再执行 1 个与本次改动直接相关的动态测试。
5. 原生界面优先使用隔离的 PySide6/offscreen 测试，直接实例化页面或主窗口并断言关键控件状态。
6. 涉及 `settings.json`、`keys.json`、TM 数据库、临时目录或用户目录的测试，优先使用：
   `powershell -ExecutionPolicy Bypass -File .\agent\testing\Run-IsolatedVenvPython.ps1 ...`
7. 动态测试产物统一写入 `.runtime\self-tests\<task-slug>\`。
8. 只有在静态检查和定向动态验证都完成后，才允许交付。

如果测试失败：
- 先修复与当前任务直接相关的问题。
- 然后重新执行同一组检查。

如果无法完成测试：
- 明确说明未执行项。
- 说明阻塞原因。
- 说明风险范围与建议下一步。

额外约束：
- 不要混用系统 Python；优先 `.\.venv\Scripts\python.exe`。
- 不要只跑静态检查就结束。
- 汇报结果时必须写明实际执行过哪些测试。
